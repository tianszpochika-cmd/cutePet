# 留空时请求同源 /api/*，开发服务器将 /api 前缀剥离并转发到本地网关。
# 正式部署也需提供等价的同源 /api 反向代理。
# 跨域部署时填写完整 HTTP(S) 网关源站，并同步配置 CORS。
VITE_GATEWAY_BASE_URL=

# 只有正式协议已发布、后端短信不再返回开发提示码，且监护流程独立核验后才可评估开启。
VITE_WEB_AUTH_READY=false
VITE_WEB_TERMS_VERSION=
VITE_WEB_PRIVACY_VERSION=
