# Production should proxy /api/* to the gateway after stripping /api.
# Only use an explicit gateway URL for a trusted HTTPS deployment.
VITE_GATEWAY_BASE_URL=

# Adult SMS login remains disabled until real delivery, identity and policy are ready.
VITE_MOBILE_AUTH_READY=false
VITE_TERMS_VERSION=
VITE_PRIVACY_VERSION=
