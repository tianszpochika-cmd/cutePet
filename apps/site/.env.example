# Configure only public HTTPS URLs that have been checked on the release environment.
# Keep the verified flags false until the destination, login and return flow are reviewed.
VITE_PUBLIC_WEB_URL=
VITE_PUBLIC_WEB_VERIFIED=false
VITE_PUBLIC_MOBILE_URL=
VITE_PUBLIC_MOBILE_VERIFIED=false
