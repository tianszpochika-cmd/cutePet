# 开发任务规划（从零到本地可测 · 功能完善 · 无 Bug）

> 状态：待评审（**选型已决议：与 cloudstudy 全栈对齐**） · 版本：v1.1 · 更新日期：2026-09-22
>
> 上游输入：`01-需求文档`（已定稿·上线合规版）、`02-设计文档`（14 份，8 定稿 10 待复审）、`03/冷启动运营计划`。
> 本文档把「本地可以测试、功能完善、无 bug」定义为**可验证的目标**，并分解为带验收标准的任务。

---

## 0. 总目标与完成定义（DoD）⭐

**一句话目标**：一条命令在本地拉起全部端与服务，演示数据可点可用，需求范围内 100% 功能可测，全部验收用例通过、无 P0/P1 缺陷。

### 0.1 「本地可以测试」标准（可客观验证）

| # | 标准 |
|---|------|
| 1 | `pnpm install && pnpm dev` 后同时可访问：用户端 Web `18580`、管理端 `18581`、官网 `18582`、移动 H5 `18583`、API 网关 `18470`（固定端口 strictPort，写入 README） |
| 2 | 内置 `pnpm seed` 演示数据：≥2 账号、≥3 宠物、30 篇文章、200 POI、50 商品、举报/投稿样例、1 个家庭组 |
| 3 | 手机验证码在 dev 模式固定（如 `123456`）或打印到控制台，无需真实短信/微信即可注册登录 |
| 4 | 数据库复用开发机就绪的本地 MySQL（13306）：独立 `cutepet_*` schema + Flyway 自动迁移；`pnpm db:reset` 一键重建 + seed |
| 5 | 每个里程碑合入后主分支可运行（不许出现「合不进去跑不起来」的中间态） |

### 0.2 「功能完善」标准

- 需求 `需求全景-功能清单.md` §5 **In Scope 全量**（A1–A5、B1–B6、C1–C7+C9、D1–D7、E1–E3、F 全部、H1–H2）逐条有对应实现 + 验收用例（见 §6 用例地图）。
- 四端 UI/UX 按已产出的设计文档实现（含动效、空态、异常态、合规页面）。
- Out-of-scope 项**确认不存在**（无购物车/订单/支付、无商业化、无青少年模式）。

### 0.3 「无 Bug」标准（退出准则，见 §5 详表）

P0（崩溃/资损/合规事故）= **0**；P1（主流程中断）= **0**；P2 ≤ 5 且全部登记挂账；Playwright E2E 全绿；两轮完整回归通过；四端手动矩阵无未决项。

---

## 1. 技术选型决议（✅ 已决议：与 `cloudstudy` 全栈一致 · 2026-09-22）

> 决策依据：cloudstudy 的运行环境在开发机已完全就绪，对齐即可零环境成本复用。
> 依据 AGENTS 约定：选型内容只写在 `docs/05-技术文档/`，不回流 docs/01、02。
> 已实地核查 `Desktop\作品\cloudstudy` 的 package.json / pom.xml / .env.example / services / database。

| 层 | 选型（= cloudstudy 实测栈） | 版本/说明 |
|----|------|-------------|
| 运行时 | **Node 20.19.5 + pnpm 10.19.0**（engines 锁定，与 cloudstudy 完全同版本） | 开发机已装 |
| 仓库 | **pnpm workspaces + Turborepo 2**（`turbo dev/build/test/lint/typecheck`） | 复用其 turbo.json 模式 |
| 前端框架 | **Vue 3 + TypeScript 5.4 + Vite 6 + vue-router 4 + Pinia 2 + @vueuse/core**（vue-tsc 类型检查） | 与 learner-web/admin-web 同栈 |
| 管理端 | **Element Plus 2.6 + @element-plus/icons-vue** | 同 admin-web |
| 移动端一期形态 | **响应式 H5**（与 cloudstudy `apps/mobile` 完全一致），固定端口独立 dev server | **厂商推送留接口**，本地 Web Notification + 站内消息模拟 L2（已知限制） |
| 官网 | 同 Vue 栈 CSR（同 official-site），预渲染 SEO 页 | |
| 共享包 | **`contracts`（OpenAPI/类型契约）+ `api-client`（生成的客户端）+ `design-tokens` + `ui`**（对标 cloudstudy 的 contracts/api-client/design-tokens/i18n） | 前后端并行前提 |
| 后端 | **Java 25 + Spring Boot 4.1.1 + Spring Cloud 2025.1.3（BOM）+ Maven 多模块**，服务由 `service-template` 起步 | 与 cloudstudy 根 pom 完全一致；JDK/Maven 环境已就绪 |
| 服务拓扑 | **按 cutePet 域裁剪**（不搬 15 个服务）：`api-gateway` + `iam`（账号/家庭/隐私）+ `pet`（档案/记录/提醒）+ `content`（资讯/UGC）+ `explore`（POI/路线/活动）+ `catalog`（用品）+ `admin-ops`（审核/治理/RBAC）+ `notification`（消息/提醒触发）+ `audit`（审计）= **9 服务**，走网关统一入口 | cloudstudy 模式 × cutePet 规模 |
| 认证 | **本地 JWT 中心（IAM 模式：RS256、issuer/JWKS 端点、refresh cookie）**；dev 短信=固定码/测试账号矩阵；微信登录 dev stub | 同 cloudstudy IAM 设计 |
| 提醒调度 | Spring `@Scheduled`（notification-service 每分钟扫描状态机） | 推送=站内+Web Notification |
| 内容安全 | **本地词库机审 + 图片规则 stub**，`ModerationService` 接口预留厂商 | 流程可测；厂商接入属部署期 |
| 搜索 | MySQL FULLTEXT（文章/作者/商品） | ES 属二期 |
| 数据库 | **本地 MySQL 8（复用 13306 实例）+ Flyway 迁移 + 每服务独立 schema（owner-schema 模式）** | 不再用 SQLite；database 名 `cutepet_*`，与 cloudstudy 库隔离 |
| 缓存/对象存储 | **复用本地 Redis（16379，keyspace 前缀 `cutepet:*`）+ MinIO（19000，bucket `cutepet-local`）** | 环境零搭建 |
| 消息队列 | **一期不引入**（cloudstudy 的 Kafka/RabbitMQ 环境就绪但 cutePet 同步链路足够）；接口层预留，需要时开关启用 | 决议 |
| 契约流程 | **contracts 先行**：`packages/contracts` 定义 → `contracts:validate/generate` 脚本 → 前端 `api-client` 生成（对标 cloudstudy scripts/contracts 模式） | |
| 测试 | **node:test（前端/脚本）+ JUnit 5 / Spring Boot Test（后端）+ Playwright ^1.63（E2E）**；Prettier + vue-tsc + turbo test/lint/typecheck 门禁 | 与 cloudstudy 一致（无 Vitest） |
| 本地端口 | **固定端口制（strictPort，不自动递增）**，避开 cloudstudy 已占用段：网关 18470、cutePet 服务 18480–18488、Web 18580 / 管理端 18581 / 官网 18582 / 移动 H5 18583 | cloudstudy 占用 18070–18099/18180–18190、13306/16379/19000/19092，不冲突 |
| Seed | `scripts/database` 模式：`db:seed` 一键演示数据 + `db:reset`（Flyway repair + 重建） | §0.1-2 |

### 1.1 机器角色与环境基线（2026-09-22 更正）

| 机器 | 角色 | 环境 |
|------|------|------|
| **本机（设计机）** | 只承担文档写作与评审，**不承担开发** | 本机实测（JDK21/Node24/infra 未启动）**仅作记录，不作开发依据** |
| **开发机（另一台电脑）** | 承担全部开发与本地测试 | **cloudstudy 环境已完全就绪**（JDK25、Node 20.19.5、pnpm 10.19.0、MySQL/Redis/MinIO 等）——这就是 cutePet 的环境基线 |

- **对齐原则**：选型与工具链一切以 cloudstudy 在开发机上的状态为准；T0.0 改为**在开发机上验证并记录基线**，不在设计机上折腾环境。
- 端口避让不变：cloudstudy 占用 18070–18190，cutePet 用 18470 / 18480–18488 / 18580–18583。

---

## 2. 仓库结构（目标态）

```
cutePet/                          # 结构对齐 cloudstudy
├── apps/
│   ├── web/                      # 用户端 Web（Vue3+Vite6，端口 18580）
│   ├── admin/                    # 管理端（+Element Plus，18581）
│   ├── site/                     # 官网 29 路由（18582）
│   └── mobile/                   # 移动 H5 57 屏（18583）
├── services/                     # Java25 + SpringBoot4 Maven 多模块（service-template 起步）
│   ├── api-gateway/              # 18470 统一入口（JWT 预校验/路由）
│   ├── iam-service/              # 账号/家庭/隐私（18480）
│   ├── pet-service/              # 档案/记录/曲线/提醒/摘要（18481）
│   ├── content-service/          # 资讯/UGC/审核队列（18482）
│   ├── explore-service/          # POI/评价/纠错/路线/活动（18483）
│   ├── catalog-service/          # 商品/清单/评测关联（18484）
│   ├── admin-ops-service/        # 审核工作台/治理/RBAC（18485）
│   ├── notification-service/     # 消息中心/提醒调度 @Scheduled（18486）
│   ├── audit-service/            # 审计日志（18487）
│   └── service-template/         # 新服务模板
├── packages/
│   ├── contracts/                # OpenAPI/类型契约 + validate/generate 脚本
│   ├── api-client/               # 契约生成的前端客户端（workspace:*）
│   ├── design-tokens/            # 视觉设计总纲令牌
│   └── ui/                       # 跨端组件
├── database/
│   ├── migrations/               # Flyway（每服务 schema 独立）
│   ├── schema/ seeds/ fixtures/  # 同 cloudstudy database 目录约定
├── scripts/                      # contracts/ database/ runtime/ dev/（对标 cloudstudy）
├── tests/ + e2e/                 # node:test + Playwright
├── infra/ deploy/                # 本地环境说明（复用 cloudstudy 基础设施）
├── docs/                         # 本仓库现有文档
├── pnpm-workspace.yaml  turbo.json  pom.xml（BOM Reactor）  .env.example
└── README.md                     # 一键启动手册（交付物）
```

---

## 3. 里程碑与任务分解（WBS）

> 规模：S ≤0.5 天 · M ≤1.5 天 · L ≤3 天（单人估点，草案可校准）。
> 每任务含**验收标准（DoD）**；状态在任务看板维护（§8）。

### M0 · 基建与契约（先行，无依赖）

| ID | 任务 | 规模 | 验收标准 |
|----|------|:--:|----------|
| **T0.0** | **开发机环境验证与基线固定**（在**开发机**执行，依据 §1.1）：① 核验与 cloudstudy 同款版本（JDK25/Maven/Node 20.19.5/pnpm 10.19.0）② 复测基础设施端口（13306/16379/19000）③ 确认 cutePet 独立 schema/Redis 前缀/MinIO bucket/端口段与 cloudstudy 无冲突 ④ 结果记录为 `05-技术文档/环境基线.md` | S | 开发机上四项验证命令全绿；**不改动 cloudstudy 自身任何配置** |
| T0.1 | monorepo 脚手架：pnpm workspace + turbo、4 个前端 app 壳、services 根 pom（BOM）+ service-template、packages(contracts/api-client/design-tokens/ui) | M | `pnpm dev` 起 4 个前端壳 + gateway `/health` 可访问；lint/typecheck 通过 |
| T0.2 | 设计令牌落地 `packages/design-tokens` + `packages/ui`：色/字阶/圆角/阴影 + 基础组件（按钮/卡片/输入/Toast/Dialog/空态） | M | demo 页展示全部令牌与组件，视觉对照视觉设计总纲 |
| T0.3 | 后端骨架：Spring Boot 按域起 9 服务（service-template）+ 网关路由 + Flyway + `/health` + 统一错误体 + 审计切面 | L | health 全绿；错误统一格式；审计切面可写 audit-service |
| T0.4 | **契约先行 v1**：`packages/contracts` 按数据设计出全量 schema/路由 → `contracts:validate/generate` → 生成 `api-client`（对标 cloudstudy scripts/contracts） | L | 契约覆盖 §6 全部用例所需接口；前端可直接用 client |
| T0.5 | seed 脚本（`scripts/database` 模式）+ `db:reset`（Flyway repair/重建）+ dev 认证捷径（固定验证码/测试账号矩阵，对标 cloudstudy B23） | M | 一键产出 §0.1-2 演示数据；两账号可登录 |
| T0.6 | CI 级本地门禁：husky + Prettier + vue-tsc/tsc + `turbo lint typecheck test` + node:test 冒烟 | S | commit 时全绿才可提交 |

### M1 · 后端-账号、家庭、合规

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T1.1 | 注册登录（手机号验证码/微信 stub、JWT 双 token、协议勾选版本记录） | M | T0.3-5 | 用例 U1 全过；协议版本落库 |
| T1.2 | 年龄分支 + 监护人同意流程（<14：监护码验证、专门规则版本、可撤回） | M | T1.1 | U2 全过；撤回后未成年账号受限 |
| T1.3 | 资料/养宠标签/换绑/找回 + 异地登录提醒事件 | M | T1.1 | U3 |
| T1.4 | 隐私能力：数据导出(JSON+核验)、注销(15 天冷静+撤销+阻断校验)、撤回同意 | L | T1.1 | U4；导出含全量个人数据；注销阻断家庭共享 |
| T1.5 | 家庭组：创建/邀请码(7天)/加入/一人一家庭/两档权限/移除解散 | L | T1.1 | U5；权限判定表逐行通过（功能设计-账号 §2） |
| T1.6 | 治理工单：举报(7 类对象)、版权通知-反通知、申诉、处置记录（L1–L4 分级执行） | L | T1.1 | U6；48h 时效字段可查；封禁生效于鉴权层 |
| T1.7 | 消息中心（5 类）+ 通知模板 | M | T1.6 | U7 |
| T1.8 | 埋点接收接口（合规：入收集清单） | S | T0.3 | 事件落库，看板可查 |

### M2 · 后端-宠物管理（生命链）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T2.1 | 宠物 CRUD + 归档/软删(30 天) + 多宠 | M | T1.1 | U8 |
| T2.2 | 7 类健康记录 + 时间线聚合 + 单位/波动规则(≥10% 校验) | L | T2.1 | U9 |
| T2.3 | 体重曲线数据接口（点位、趋势、≥5% 高亮标记） | M | T2.2 | U10 |
| T2.4 | 提醒状态机 + Spring `@Scheduled` 调度（notification-service 每分钟扫描：到期触发、3 天过期二次、家庭同步、北京时间） | L | T2.1, T1.5 | U11；状态机单测（JUnit）覆盖全转移 |
| T2.5 | 健康摘要数据组装（勾选模块、隐私字段过滤） | S | T2.2 | U12；输出不含手机号 |

### M3 · 后端-资讯与 UGC

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T3.1 | 文章/频道/专题/榜单/快讯 CRUD + 推荐位配置 | L | T0.4 | U13 |
| T3.2 | 投稿状态机（先审后发、驳回意见、3 次不过暂停 7 天）+ 创作数据 | L | T1.1 | U14；状态机单测 |
| T3.3 | 审核队列 API（多类型队列、处理中锁、SLA 时长、批量） | M | T3.2, T1.6 | U15 |
| T3.4 | 互动（赞/藏/评/关注）+ 评论机审 stub（疑似仅自见） | M | T3.1 | U16 |
| T3.5 | 搜索（MySQL FULLTEXT：文章/作者/商品）+ 空结果推荐 | M | T3.1 | U17 |

### M4 · 后端-探索

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T4.1 | POI 模型 + 列表/详情 + 距离计算(5/10/20km) + 综合排序(0.6/0.4) | L | T0.4 | U18 |
| T4.2 | 评价：三维度、防刷限额(1天1条/设备≤5)、新号先审后计、图片重复检测 stub | L | T4.1 | U19 |
| T4.3 | 纠错工单（同类合并、采纳回写、双向通知） | M | T4.1 | U20 |
| T4.4 | 路线（选点数据结构、先审后发） | M | T3.3 | U21 |
| T4.5 | 活动/领养 + 报名（**告知同意凭证落库**、≤90 天保留标记）+ 领养风控词 | M | T1.6 | U22 |

### M5 · 后端-用品

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T5.1 | 商品库（**兽药/处方类目黑名单**）+ 筛选 + 热度(30 天浏览+收藏×3) | M | T0.4 | U23 |
| T5.2 | 清单/评测双向关联 + CSV 批量导入 | M | T3.1 | U24 |

### M6 · 后端-治理、看板、RBAC

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T6.1 | **RBAC 32 权限点引擎** + 预置 6 角色 + 自定义角色 + 高危审计 | L | T1.1 | U25；权限点逐个断言 |
| T6.2 | 管理端数据接口（看板聚合、SLA/积压、举报时效） | M | T1-5 各域 | U26 |
| T6.3 | 审计日志查询（操作者/IP/前后值） | S | T0.3 | U27 |

### M7 · 前端-用户端 Web（依赖契约，可与 M1–M6 并行，先 mock）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T7.1 | 认证流全页（登录/验证码/年龄分支/监护人/协议勾选/闸门回跳） | L | T0.2, 契约 | U30；回跳续做原操作 |
| T7.2 | L2 核心：宠物列表/建档向导/详情 4Tab/记录表单/曲线/提醒中心 | L | T7.1 | U31；本地暂存同步可用 |
| T7.3 | 首页聚合（提醒条/宠物卡/好物/探索/资讯流） | M | T7.2 | U32 |
| T7.4 | 资讯端（Feed/频道/详情/进度条/评论/搜索/关注/收藏分组） | L | T7.1 | U33 |
| T7.5 | 创作中心（编辑器自动存/状态/数据/作者主页） | L | T7.4 | U34 |
| T7.6 | 探索（地图页双视图/详情/评价/纠错/路线/活动报名） | L | T7.1 | U35；地图用本地 SVG/Canvas 底图（持证 SDK 属部署期） |
| T7.7 | 用品（频道/列表/详情/清单/收藏） | M | T7.1 | U36 |
| T7.8 | 家庭（管理/邀请/加入）+ 个人中心/收藏/消息 | L | T7.1 | U37 |
| T7.9 | 设置与合规页（隐私/通知/导出/注销/帮助/规则五页/举报面板/封禁页） | L | T7.1 | U38；注册必须过协议勾选 |
| T7.10 | 动效落地（入场/曲线绘制/记成功/骨架空错三态） | M | T7.2-4 | 对照动效规范逐条过 |

### M8 · 前端-管理端

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T8.1 | 登录 + 侧边导航 + 权限指令（无权限项隐藏） | M | T6.1 | U40 |
| T8.2 | **审核工作台**（队列/详情抽屉/模板驳回/处理中锁/批量/快捷键） | L | T3.3 | U41 |
| T8.3 | 内容/商品/POI/纠错/路线活动运营页 | L | M3-M5 | U42 |
| T8.4 | 用户治理（封禁分级/投稿权/家庭纠纷/申诉） | M | T1.6 | U43 |
| T8.5 | 看板 + 审计日志页 + 角色权限管理页 | L | T6.1-3 | U44 |

### M9 · 前端-官网（29 路由）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T9.1 | 全局框架（吸顶导航/页脚含备案位/双 CTA/搜索） | M | T0.2 | U45 |
| T9.2 | 首页 + 产品总览 + 4 产品页 + 家庭长页 | L | T9.1 | U46；对照官网 UX §1–7 |
| T9.3 | 场景×3 + 资讯/活动列表详情 + 下载页 + 搜索 | L | T9.1 | U47 |
| T9.4 | 品牌三页 + 帮助中心 + **法律五页** + 404 + Campaign 模板 | L | T9.1 | U48；法律页锚点目录可用 |
| T9.5 | 官网动效（逐词入场/滚动级联/count-up/轮播）+ reduced-motion | M | T9.2 | U49 |

### M10 · 前端-移动 H5（57 屏）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T10.1 | 移动骨架：4-Tab/导航栈/Sheet/手势返回/安全区 | L | T0.2 | U50 |
| T10.2 | 认证流 + 首页 + 记录快捷流（单手可达走查） | L | T7.1-3 组件复用 | U51 |
| T10.3 | 资讯/创作 + 探索地图 + 用品 + 家庭 + 我的设置全量 | L | M7 | U52；57 屏路由全通 |
| T10.4 | 通知：Web Notification + 站内；厂商推送接口 stub | M | T2.4 | U53；提醒到端可点 |
| T10.5 | 移动动效（跟手返回/Sheet 拖拽/记成功/reduced-motion） | M | T10.1 | U54 |

### M11 · 质量工程（贯穿 + 收口）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T11.1 | 后端单测（JUnit 5 / Spring Boot Test）+ 前端 node:test：4 个状态机 + 权限点 + 防刷/波动等规则函数 | L | M1-6 | 核心域覆盖率 ≥80% |
| T11.2 | E2E（Playwright）：L1 激活、L2 记录、L4 投稿审核、L5 探索、注销/举报 | L | M7/M8 | 全绿（§6） |
| T11.3 | 手动测试矩阵：四端 × Chrome/Safari/Edge × 375/768/1200 | M | M7-10 | 缺陷全部入池定级 |
| T11.4 | 性能与无障碍基线：首屏/LCP/对比度/键盘走查 | M | M7-10 | 达到动效规范 §7 与视觉总纲 §7 |
| T11.5 | 合规自查：对照上线合规审查报告 §5 逐条核 | S | 全部 | 清单全过（法务文案外） |

### M12 · 除虫与本地交付（达成「无 Bug」）

| ID | 任务 | 规模 | 依赖 | 验收标准 |
|----|------|:--:|------|----------|
| T12.1 | 缺陷池收敛：复现→定级→修复→回归（§5 流程），连续 3 天无新增 P0/P1 | L | T11.2-3 | §5 出口准则全满足 |
| T12.2 | 两轮完整回归（E2E + 手动矩阵） | M | T12.1 | 两轮 100% 通过 |
| T12.3 | 交付物：根 README（一键启动/地址表/演示账号/FAQ）、已知问题清单（P2 挂账） | S | T12.2 | 按 README 从零跑通 ≤10 分钟 |
| T12.4 | 本地验收演示脚本：按 L1–L9 链路走查录像/清单签字 | S | T12.3 | 负责人验收通过 = **目标达成** |

---

## 4. 执行顺序与并行策略

```
M0 基建+契约 ──┬─→ M1 后端账号 ─┬→ M2 管理 → M3 资讯 → M4 探索 → M5 用品 → M6 治理看板
（严格先行）   │                │        （后端可 2 人按域并行）
               ├─→ M7 Web（契约+mock 先行，接口就绪切真）
               ├─→ M8 管理端（依赖 T6.1 权限 + 各域 API）
               ├─→ M9 官网（只读内容，最早可完成）
               └─→ M10 H5（复用 M7 组件，后置 20% 工期）
                        ↓
              M11 质量工程（测试用例与开发同步写，不留到最后）
                        ↓
              M12 除虫 → 两轮回归 → 交付验收 ✅
```

- **契约先行**是并行前提：T0.4 完成前，前端只允许写静态页与组件。
- 单人执行时按 M0→M1→M2→M7(核心)→M3→M7(资讯)→M4/M5→M8→M9→M10→M11→M12 串行，工期约为并行方案 1.8 倍。
- 每个里程碑结束：`git tag mN` + 主分支可运行 + 冒烟通过（M0.6 门禁）。

## 5. Bug 治理与「无 Bug」退出准则

**分级**

| 级 | 定义 | 例 |
|----|------|-----|
| P0 | 崩溃/数据错误/合规事故/资金(未来) | 注销后仍可登录、他人档案越权可见、举报入口缺失 |
| P1 | 主流程中断、无绕过 | 无法建档、投稿卡死、审核操作失效 |
| P2 | 功能瑕疵有绕过 | 空态文案错位、排序不精确 |
| P3 | 观感 | 间距 1px、文案错别字 |

**流程**：复现录屏 → 入池定级 → 修复（附回归用例）→ QA 回归 → 关闭。P0/P1 当日修复；修复引入的新缺陷记为回归缺陷，责任随源任务。

**出口准则（全部满足才可宣称「无 Bug」）**

1. 缺陷池：P0 = 0、P1 = 0、P2 ≤ 5（全部登记进《已知问题清单》并给出计划）、P3 ≤ 15 或清零；
2. E2E（T11.2）连续两次运行 100% 通过；
3. 手动矩阵（T11.3）两轮 100% 通过，无未决项；
4. 核心域单测绿 + 覆盖率达标（T11.1）；
5. 14 天内无新增 P0/P1（观察窗，短周期项目可缩至 7 天，评审定）；
6. 合规自查（T11.5）通过 —— 法务文案未到位项按外部依赖挂账，不算缺陷。

## 6. 验收用例地图（需求追溯 · 摘要）

| 链 | 用例组 | 关键断言 |
|----|--------|----------|
| U1–U7 | 账号/家庭/合规 | 协议未勾选不可注册；<14 走监护人；一人一家庭；导出含全量；注销阻断共享；封禁后鉴权拒绝 |
| U8–U12 | 宠物管理 | 波动≥10% 二次确认；疫苗记录生成提醒；cron 触发→消息；3 天过期二次；摘要无手机号 |
| U13–U17 | 资讯 UGC | 先审后发（未审不可见）；3 驳暂停 7 天；评论疑似仅自见；搜索高亮 |
| U18–U22 | 探索 | 1 天 1 条防刷；新号评价先审；纠错采纳回写 POI；报名**同意凭证**落库 |
| U23–U24 | 用品 | 兽药类目不可建；热度=浏览+收藏×3；关联商品删除降级 |
| U25–U27 | 治理 | 32 权限点逐个 403/放行断言；审计含 IP 与前后值 |
| U30–U54 | 四端 UI | 对照各端 UX 规格逐屏走查 + 动效/reduced-motion 项 |
| 全链 | L1–L9 | E2E 端到端各 1 条 happy path + 1 条异常 path |

> 完整用例表（前置/步骤/期望）在 M11.1 前由 Agent 展开为 `05-技术文档/测试用例.md`（任务 T11.x 的一部分）。

## 7. 风险与应对

| 风险 | 应对 |
|------|------|
| H5 推送非厂商通道，L2 召回打折 | 接口 stub 已留（T10.4）；App 形态启动时无缝换通道——**属已知限制，写入交付 README** |
| 地图无持证 SDK（本地测试） | 本地用静态底图/Canvas 实现交互；持证 SDK 切换集中在 `MapProvider` 接口（选型属部署期） |
| 机审 stub ≠ 生产能力 | 流程（先审后发/疑似转人工）可测；厂商接入为部署期配置项 |
| 四端范围大、单人执行工期长 | 里程碑可裁剪顺序：Web 核心 → 管理端 → 官网 → H5；每阶段末可演示 |
| 设计文档 10 份待复审 | 复审与 M0–M1 并行进行，设计变更走评审记录，不阻塞基建 |
| 9 服务对小项目仍偏重 | 每服务保持「薄」（service-template + 单域）；本地 `turbo dev`/统一脚本一键起；若工期吃紧可先并 3 组（iam/content+catalog+explore/admin-ops+audit+notification），接口契约不变 |
| 与 cloudstudy 共用本地基础设施（MySQL/Redis/MinIO 端口） | cutePet 用独立 schema/bucket/Redis 前缀隔离；`.env` 独立不覆盖 cloudstudy；端口段 18470/18480–18488/18580–18583 避让其 18070–18190 段 |

## 8. 任务看板与执行约定

- 任务状态：`待开始 → 进行中 → 自测通过 → 待验收 → 完成`；看板落盘 `05-技术文档/任务看板.md`（开工时建）。
- 每任务完成 = 代码合入 + 自测记录 + 关联验收用例变绿 + 更新看板。
- Agent 执行纪律：一次领一个任务（对齐 AGENTS「边界清晰」）；产出必须落文件；遇需求/设计缺口 → 记 `评审记录.md`，不擅自改范围。
- 变更控制：本文任务可增删，但 **§0 DoD 与 §5 出口准则不可降低**。

---

## 9. 评审请求（开工前）

1. ~~技术选型决议~~ ✅ **已决策（2026-09-22）：与 cloudstudy 全栈一致**（§1 v1.1，含 9 服务拓扑裁剪、端口避让、基础设施复用方案；移动 H5 与 cloudstudy `apps/mobile` 同形态，推送为已知限制）。
2. 单人还是多人执行（决定 §4 串行/并行与工期）。
3. §5 观察窗 14 天 vs 7 天。
4. 确认后：建任务看板 → 从 T0.1 开工。
